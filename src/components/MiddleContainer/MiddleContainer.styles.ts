import styled from "styled-components";

export const MiddleContainerWrapper = styled.div`
  background-color: ${(props) =>
    props.theme.colors[props.theme.defaultTheme].secondary};
  width: 100%;
`;
